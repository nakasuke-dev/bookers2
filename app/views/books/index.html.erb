<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <%= render 'users/userInfo', user: current_user %>
      <%= render 'books/new', book: Book.new %>
    </div>
    <div class="col-md-8 offset-md-1">
      <%= render 'books/index', books: @books, h2: 'Books' %>
    </div>
  </div>
</div>